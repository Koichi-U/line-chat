<% @chats.each do |c| %>
  <div class="chat">
    <%= l.lineusers_id %>
    <%= l.message %>
  </div>
<% end %>

<%= form_for @chat do |f| %>
  <div class="field">
    <%= f.message :message %>
    <%= f.text_field :message, :size => 140 %>
  </div>
  <%= f.submit "message" %>
<% end %>