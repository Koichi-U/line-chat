<% if !messages.nil? && !lineuser.nil? %>
  <% @day = DateTime.new(0, 1, 1, 0, 0) %>
  <%= @day %>
  <%= render partial: 'message', collection: messages, as: "message", locals: { lineuser: lineuser, day: @day } %>
  
<% else %>
<% end %>